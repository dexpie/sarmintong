import React from 'react';

const Events = () => {
    return (
        <section id="events" className="events-section">
            <div className="container">
                <h2>Upcoming Events</h2>
                <p>Check out our latest events and join us for exciting activities!</p>
            </div>
        </section>
    );
}

export default Events;
